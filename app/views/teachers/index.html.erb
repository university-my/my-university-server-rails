<%= render "records/links" %>
<hr>
<p id="notice"><%= notice %></p>

<h1><%= @university.short_name %> - Викладачі</h1>

<table>
  <thead>
    <tr>
      <th>Імя</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><%= teacher.name %></td>
        <td><%= link_to 'Показати', university_teacher_url(@university.url, teacher.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<hr>
<%= render "records/links" %>