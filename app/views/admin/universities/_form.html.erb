<%= form_with(model: admin_university, local: true) do |form| %>
  <% if admin_university.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_university.errors.count, "error") %> prohibited this admin_university from being saved:</h2>

      <ul>
      <% admin_university.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
